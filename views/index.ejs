<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item List</title>
</head>
<body>
    <h1>My store List</h1>
    <ul style="list-style: none;">
        <% items.forEach(b => { %>
            <li style="margin-bottom: 1rem;">
                <form style="display: inline; margin-right: 1rem;" action="/items/<%= b._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                </form>
                <a style="margin: 0 1rem 0 0;" href="/items/<%= b._id %>/edit">EDIT</a>
                <a href="/items/<%= b._id %>">
                    <%= b.name %>
                </a>
                - 
                <p style="display: inline; margin-left: .5rem;">There are currently <%= b.qty %> of these available</p>
            </li>
        <% }) %>
    </ul>
    <a href="/items/new">Add a New Item</a>
</body>
</html>
